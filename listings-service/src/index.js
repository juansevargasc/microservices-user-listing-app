import "@babel/polyfill";
import"#root/db/connection"; // This is the connection to the database
import "#root/server/startServer"; // This is the server that will be started

//console.log("LISTINGS MS wErking!")